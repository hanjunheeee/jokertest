/**
 * @file gameModeIndicatorAssets.js
 * @desc GameModeTypeIndicator(멀티플레이 진입 후 상단 모드 배지)가 사용하는
 * 에셋 경로와 라벨 텍스트 모음
 */

/** MultiplayEntryPage 상단 — 선택된 게임 모드 탭(활성) 이미지 */
export const GAME_MODE_INDICATOR_ASSETS = {
  tabActive: "/button/인게임설정 옵션버튼(활성화).png",
}

/** GameModeTypeIndicator — 모드 id별 표시 라벨 */
export const GAME_MODE_INDICATOR_LABELS = {
  multi: "멀티플레이",
  single: "싱글플레이",
}
