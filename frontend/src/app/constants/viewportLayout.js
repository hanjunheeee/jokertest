/** 게임 UI 가운데 영역 최대 너비(px) — 넓은 화면에서 좌·우 배너 슬롯(검은 여백) 확보 */
export const GAME_VIEWPORT_MAX_WIDTH_PX = 1280

/** ViewportShell·index.css에서 공유하는 CSS 변수 이름 */
export const GAME_VIEWPORT_MAX_WIDTH_VAR = "--game-viewport-max-width"
