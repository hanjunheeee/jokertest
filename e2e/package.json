{
  "name": "joker-e2e",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Playwright 멀티 클라이언트 10일차 시나리오 재생 스크립트",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:helpers": "node --test \"lib/__tests__/*.test.js\"",
    "install:browsers": "playwright install chromium",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
